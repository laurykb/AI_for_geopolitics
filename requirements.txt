# Dependances CORE pour LANCER le backend (`python serve.py`).
#
# `pyproject.toml` reste la SOURCE CANONIQUE (ce fichier en est un miroir pratique
# pour `pip install -r requirements.txt`).
#
# Pour TESTER (pytest + ruff), ajouter les deps de dev :
#   pip install -r requirements-dev.txt
#
# Extra RAG (embeddings/reranking reels, tire torch) — non requis pour les tests
# offline ni pour jouer :  pip install sentence-transformers>=2.2

pydantic>=2.5       # modeles de domaine + validation
ollama>=0.4         # client du backend d'inference local
numpy>=1.26         # index vectoriel in-memory (RAG)
rank-bm25>=0.2      # retrieval lexical (RAG)
fastapi>=0.110      # API backend
uvicorn>=0.29       # serveur ASGI
httpx>=0.27         # client HTTP (TestClient FastAPI)
