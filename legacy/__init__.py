"""Interface Streamlit archivée (Phase 5 → refonte R4).

L'UI vivante est le front Next.js (`web/`) branché sur l'API FastAPI (`app/`).
Ce paquet est conservé en lecture : `legacy.game` (contrôleur de partie, pur
Python, encore testé) et `legacy.app` (coquille Streamlit du théâtre live).
"""
