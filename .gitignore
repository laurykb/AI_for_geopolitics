# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Secrets & env (NE JAMAIS committer)
.env
*.env
.mcp.local.json
# Secret K8s réel dérivé de infra/k8s/secret.example.yaml (jamais committé)
infra/k8s/secret.yaml

# Modèles & données lourdes
*.gguf
*.safetensors
models/
# Exception : les assets 3D publics (glTF/glb) sont versionnés (models/ ci-dessus vise les poids ML).
!web/public/models/
!web/public/models/**
data/corpus/
chroma/
*.sqlite
*.db
*.db-wal
*.db-shm

# Textures NASA source 8K (hors build ; voir scripts/downsample-textures.mjs)
/nasa_texture/

# Artefacts temporaires de benchmark, rendu PDF et QA locale
tmp/

# Node / web (le projet Node vit dans web/ ; voir aussi web/.gitignore — défensif ici)
node_modules/
.next/

# Archives (copies de travail, exports tar — ne jamais committer)
*.tar
*.tgz
*.tar.gz

# OS / IDE
.DS_Store
.idea/
.vscode/

# Claude Code local (perso, non partagé)
.claude/settings.local.json
.tmp/
