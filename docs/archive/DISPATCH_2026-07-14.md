# Dispatch Cowork ↔ Claude Code — session du 2026-07-14

> Ce qui a été fait aujourd'hui côté Cowork, et les prompts de session prêts à coller
> dans Claude Code pour la suite. Ordre conseillé : CC-1 → CC-2 → CC-3 → CC-4 → CC-5.

## Fait aujourd'hui (Cowork)

**Code livré, vérifié (tsc + eslint verts), écrit dans le repo :**

- `web/src/components/ui.tsx` — nouveaux primitives `ConfirmDialog` (remplace les
  `confirm()` natifs) et `Skeleton`.
- `web/src/app/games/[id]/page.tsx` (théâtre) — auto-scroll intelligent du transcript
  (+ bouton « ↓ Revenir au direct »), la parole du joueur n'est plus jamais perdue
  (bannière + réessai en cas d'échec du POST), pile d'avis compactée (motion /
  suspensions / campagne / dérive en pastilles dépliables dès 2 avis), squelette de
  chargement, forfait via ConfirmDialog, « Publier le récit » avec spinner/disabled,
  transcript sticky à hauteur adaptative, grille carte/transcript dès `lg` (1024px),
  annonces lecteur d'écran (`aria-live` sr-only), uuid raccourci dans le header.
- `web/src/app/lobby/page.tsx` — bouton « Jouer » désactivé expliqué (« choisis encore
  N États… »), toggle Dérive désactivé hors Classique (et l'intention n'est plus
  envoyée à tort au backend), les réglages de l'étape 1 voyagent vers `/campagne` en
  query params (`rounds`, `difficulty`, `drift`, `free`), échec de `getSources`
  signalé, la Difficulté explique enfin son effet réel.
- ⚠ Vérifié : `tsc --noEmit` + `eslint` sur les 3 fichiers. **À faire chez toi** :
  `npm run test && npm run build` (non exécutables dans mon environnement), puis
  commits atomiques (suggestion : 3 commits, un par fichier ci-dessus).
- 🧹 `web/_to_delete/` : artefacts temporaires de ma session — à supprimer.

**Design & specs :**

- Mascotte originale « Petit Kairos » (nom à valider) : `web/public/mascotte/`
  (`mascotte.svg`, `mascotte-tete.svg`, PNG 2x transparents) + philosophie de design
  (`docs/design/PHILOSOPHIE_MASCOTTE.md`). Inspirée de l'énergie chibi/inventeur que tu
  aimes, **sans copier Ekko** (personnage Riot protégé) : silhouette, tenue, accessoires
  et regard sont originaux (globe tenu en main, lunettes d'inventeur, palette or/cyan).
- `docs/specs_jeu/spec_g13_tutoriel_mascotte.md` — visite guidée dès la connexion.
- `docs/specs_jeu/spec_g14_reglages.md` — langue / confort / compte.
- `docs/specs_jeu/spec_g15_frise.md` — frise chronologique fin + replay.

## Sessions Claude Code prêtes à lancer

### CC-1 — Visite guidée + mascotte (G13)

```
Lis docs/specs_jeu/spec_g13_tutoriel_mascotte.md et implémente la visite guidée :
TourProvider (contexte + moteur d'étapes pur et testé dans web/src/lib/tour.ts),
bulle ancrée sur [data-tour=...] avec la tête mascotte (web/public/mascotte/
mascotte-tete.svg), web/src/data/tour.json (12 étapes, textes provisoires courts —
Cowork les réécrira), attributs data-tour sur accueil/lobby/théâtre/marché/
informations, flag tour_done (profil sinon localStorage), partie de démo jetable
(spectateur, non classée, exclue des listes), entrée « ? » dans le header, mascotte
compagnon coin bas-droit (désactivable, prefers-reduced-motion respecté).
Tests vitest sur le moteur (next/skip/resume, cible manquante sautée).
Termine par : ruff/eslint propres, npm run test && npm run build verts, petit diff.
```

### CC-2 — Réglages : front (G14 §1-3 côté client)

```
Lis docs/specs_jeu/spec_g14_reglages.md et implémente le front : page /reglages
(trois panneaux), hook useT + dictionnaires web/src/i18n/fr.json et en.json branchés
sur accueil/lobby/header/fin/reglages (le reste suivra), état perf plein/confort/léger
via classe sur <html> + variantes dans globals.css (voir le tableau de la spec),
formulaires changement de mot de passe et suppression de compte (ConfirmDialog du kit,
redirection accueil après suppression). Persistance : profil si dispo, sinon
localStorage. Tests vitest : useT (repli FR), mapping perf. Lint + build verts.
```

### CC-3 — Réglages : backend langue + compte (G14 §1 et §3 côté API)

```
Lis docs/specs_jeu/spec_g14_reglages.md (sections Langue et Compte) et implémente le
backend : champ language ("fr"|"en") sur la partie (POST /api/games + stockage +
GameDetail), consigne de langue injectée dans les prompts des agents LLM, du GM, du
juge et du narrateur (une partie garde sa langue de création), endpoints changement de
mot de passe et DELETE /api/players/{id} (anonymiser owner_id des parties publiées,
purger le reste, invalider la session). Tests pytest : partie en=prompts avec consigne
anglaise ; suppression anonymise et purge ; ruff propre.
```

### CC-4 — Frise chronologique (G15)

```
Lis docs/specs_jeu/spec_g15_frise.md et implémente EventTimeline : mapping pur
rounds→crans/badges dans web/src/lib/timeline.ts (testé), composant frise (courbe U en
fil, pastilles teintées par delta U, badges motion/suspension/flash/traité, overflow-x,
ol accessible, flèches clavier), intégration /games/[id]/fin (panneau relecture du
round via EventCard/VerdictPanel existants) et /games/[id]/replay (le clic pilote le
round affiché, même sémantique onSelect que StageBand). Pas de nouvel endpoint.
Tests + lint + build verts.
```

### CC-5 — Reliquats G10 (après CC-1)

```
Lis docs/specs_jeu/spec_g10_campagne.md : implémente le chapitre 0 « tutoriel scripté »
(tutorial.json data-driven, guidage in-game, imperdable, accessible seul depuis le
lobby « Apprendre à jouer ») en réutilisant le TourProvider de G13 pour le guidage UI.
Cowork fournira les textes des étapes — mets des placeholders clairs (TODO_COWORK).
```

## Reste côté Cowork (prochaines sessions ici)

- Textes des 12 étapes de la visite + des étapes du chapitre 0 (après CC-1/CC-5).
- Traduction EN des dictionnaires i18n (après CC-2).
- Variantes d'humeur de la mascotte (pointe du doigt, applaudit, réfléchit).
- Les 3 fiches historiques sourcées de G10 (ch. 4-6) et l'équilibrage G3.
- Passe visuelle sur la frise G15 une fois rendue.
